const scores = [55,65,82]

// Destructuring
const [s1, , s3] = scores

console.log(s1,s3)

const player = {
    name: 'Messi',
    club: 'Bacelona'
}

// Destructuring
const {name, club} = player

console.log(name)
console.log(club)